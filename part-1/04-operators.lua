-- or
-- and
-- <, >, <=, >=, ~= (not equal), ==
-- .. (string concat)
-- +, -
-- *, /, %
-- not, # (length), - (unary negation)
-- ^ (power)

-- compound assignment is not supported
--   i.e. i += 1

-- assigment syntax is limited;
--   i.e. foo(a = 1) is not allowed
